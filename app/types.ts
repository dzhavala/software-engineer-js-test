export type CanvasDimensions = {
    width: number;
    height: number;
}

export type ImageDetails = {
    data: string | null | undefined,
    x: number;
    y: number;
    width: number;
    height: number;
}